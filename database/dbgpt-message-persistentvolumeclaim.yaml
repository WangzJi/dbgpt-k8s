apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: dbgpt-message
  name: dbgpt-message
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
